# Production: multiple Uvicorn workers for concurrent requests (see SCALABILITY.md).
# Render sets PORT automatically.
web: gunicorn server:app -k uvicorn.workers.UvicornWorker -w 4 --bind 0.0.0.0:$PORT --timeout 120 --graceful-timeout 30
